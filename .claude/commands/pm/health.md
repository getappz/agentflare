---
description: Team health scorecard — velocity, WIP, stuck items, bottlenecks (read-only)
argument-hint: [window-weeks]
---

Load the `pm` skill (Skill tool, name "pm") and run its `/pm:health` workflow
exactly as written there. Window argument: "$ARGUMENTS" (weeks; empty means the
default 4). Read-only — never mutate items.
