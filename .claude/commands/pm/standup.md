---
description: Daily activity digest — Done / In progress / Stuck over the current project's items (read-only)
argument-hint: [cutoff-hours]
---

Load the `pm` skill (Skill tool, name "pm") and run its `/pm:standup` workflow
exactly as written there. Cutoff argument: "$ARGUMENTS" (hours; empty means the
default 24). Read-only — never mutate items.
