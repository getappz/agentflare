{
  "name": "agentflare-web",
  "private": true,
  "version": "1.0.0",
  "description": "Landing site for agentflare — agentflare.dev (Cloudflare Workers + static assets)",
  "scripts": {
    "dev": "wrangler dev",
    "build:docs": "aube run build --filter agentflare-docs",
    "copy:docs": "rm -rf public/docs && cp -R ../docs-site/dist public/docs",
    "deploy": "aube run build:docs && aube run copy:docs && wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
