# wrangler local state / cache — never commit (may hold account/session data)
.wrangler/

# deps
node_modules/

# Generated Starlight build output for agentflare.dev/docs — built fresh by
# `aube deploy` (`aube run build:docs` + `aube run copy:docs`), not committed.
/public/docs/
